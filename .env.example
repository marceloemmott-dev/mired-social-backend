# DATABASE_URL configuration
# ---------------------------
# Esta variable es CRÍTICA. Debe ser una cadena de conexión válida de PostgreSQL.
# Se recomienda usar Neon (Serverless Postgres) para este proyecto.

# Ejemplo con Neon (Recomendado):
# Crea una cuenta en https://neon.tech, crea un proyecto y copia la Connection String (Pooled o Direct).
DATABASE_URL="postgresql://usuario:password@ep-cool-site.us-east-2.aws.neon.tech/neondb?sslmode=require"

# Puerto en el que correrá la aplicación (Por defecto 3000)
PORT=3000

# (Futuro) Configuración de JWT
# JWT_SECRET="tu_secreto_super_seguro"
